import React from 'react'

export default function TextCardProyect({textCard}: {textCard: string}) {
  return (
    <p className=" font-inconsolata text-muted text-xs font-semibold ">{textCard}</p>
  )
}
